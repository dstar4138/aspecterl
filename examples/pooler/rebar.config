%% Includes top level git directory (normal usage should just point to github.
{deps, [
    {aspecterl, ".*", {git, "../../.."}}
]}.

%% Make sure AspectErl is loaded as a Library in Rebar.
{lib_dirs, ["deps/aspecterl"]}.

%% Must compile aspects first.
{erl_first_files, ["src/pool_aspect.erl"]}.
